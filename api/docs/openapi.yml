openapi: 3.1.1
info:
  title: REST API
  version: 0.0.0
  license:
    name: Proprietary
    identifier: Proprietary

servers:
  - url: /api

security:
  - BearerAuth: []

paths:
  /tasks:
    $ref: "./paths/tasks.api.yml"
  /tasks/{id}:
    $ref: "./paths/task.api.yml"

components:
  schemas:
    Task:

      $ref: ".//schema/task.yml"
  responses:
    Error400:
      $ref: "./schema/error.yml"
    Error401:
      $ref: "./schema/error.yml"
    Error403:
      $ref: "./schema/error.yml"
    Error404:
      $ref: "./schema/error.yml"
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
