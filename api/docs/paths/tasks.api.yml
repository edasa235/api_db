get:
  /tasks:
  operationId: getTasks
  summary: Get all tasks
  responses:
    "200":
        description: OK
        content:
            application/json:
            schema:
                type: array
                items:
                $ref: '../schema/task.yml'
